{
  "name": "asset-cache-proxy",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "prettier --write src",
    "format:check": "prettier --check src"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "prettier": "^3.5.3",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
